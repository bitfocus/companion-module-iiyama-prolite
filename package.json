{
  "name": "iiyama-prolite",
  "version": "2.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bitfocus/companion-module-iiyama-prolite.git"
  },
  "private": false,
  "scripts": {
    "dev": "yarn build:watch",
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "build:main": "tsc -p tsconfig.build.json",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "lint": "eslint . --ext .ts"
  },
  "prettier": "@companion-module/tools/.prettierrc.json",
  "dependencies": {
    "@companion-module/base": "~1.6.0",
    "node-arp": "^1.0.6",
    "promise-socket": "^7.0.0",
    "queue-promise": "^2.2.1",
    "wake_on_lan": "^1.0.0"
  },
  "devDependencies": {
    "@companion-module/tools": "^1.5.2",
    "@types/node": "^18.19.3",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
